# Rebuild WebUI in SemiAI reference style

## Status

Deprecated historical task.

This PRD applied to the deprecated React/Semi WebUI. The concrete paths,
component names, SemiAI visual target, and implementation plan have been
removed so they are not reused for the current `web/` UI.

Use current `web/` sources and current WebUI planning artifacts for any new UI
work. This document remains only as a record that a previous visual rebuild
existed.
