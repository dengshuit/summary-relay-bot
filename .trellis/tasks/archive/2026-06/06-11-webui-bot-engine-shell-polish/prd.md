# Fix WebUI Bot, Engine, and Shell polish

## Status

Deprecated historical task.

This PRD targeted polish work for the deprecated React/Semi frontend. Its
AppShell, page paths, and Semi-style remediation notes are stale for the current
`web/` UI.

The backend capability note that `POST /api/bot` can create a bot instance may
still be useful as API history. UI implementation guidance must come from the
current `web/` sources.
